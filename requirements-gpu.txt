# GPU PyTorch for Qwen2.5-VL-3B AWQ inference.
# Install after base requirements, replacing CPU torch:
#   pip install -r requirements.txt
#   pip uninstall torch -y
#   pip install -r requirements-gpu.txt
#
# Adjust the cu124 index URL to match your CUDA version:
#   cu118, cu121, cu124, cu126 — see https://pytorch.org/get-started/locally/

--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.4.0
